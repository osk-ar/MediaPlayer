<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MediaPlayer" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MediaPlayer" parent="Base.Theme.MediaPlayer" />

    <style name="Widget.Neumorph.ButtonCircle" parent="Widget.AppCompat.Button">
        <item name="android:minHeight">50dp</item>
        <item name="android:minWidth">50dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center</item>
        <item name="neumorph_shapeAppearance">@style/ShapeAppearance.Neumorph.ButtonCircle</item>
    </style>

    <style name="Widget.Neumorph.ImageCircle" parent="Widget.Neumorph.ImageView">
        <item name="android:minHeight">50dp</item>
        <item name="android:minWidth">50dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center</item>
        <item name="neumorph_shapeAppearance">@style/ShapeAppearance.Neumorph.ButtonCircle</item>
    </style>

    <style name="ShapeAppearance.Neumorph.ButtonCircle">
        <item name="neumorph_cornerFamily">oval</item>
    </style>


</resources>
